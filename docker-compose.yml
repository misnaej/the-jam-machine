services:
  app:
    build: .
    ports:
      - "7860:7860"
    environment:
      - HF_TOKEN
    volumes:
      - ./output:/app/output
      - hf-cache:/root/.cache/huggingface

volumes:
  hf-cache:
